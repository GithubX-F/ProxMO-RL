# Read the Docs configuration file
# See  for details

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    rust: "1.70"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements-docs.txt
    - method: pip
      path: .
